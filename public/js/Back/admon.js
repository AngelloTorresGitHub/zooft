$(document).ready(function () {
    $('body').on('click', '.save', function () {
        
        $('dniModal').val('');
        $('emailModal').val('');
        $('nombreModal').val('');
        $('apellidoModal').val('');
        $('zonaModal').val('');
        $('departamentoModal').val('');
        $('loginModal').val('');
        $('passwdModal').val('');

        //alert('Nuevo empleado generado correctamente');
        
    });
});
